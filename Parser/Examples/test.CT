Program Nom_program
PDEC
    //declarer le constante
    @define Pint x=5;
    @define Pint y=5;
    //declarer les variable
     j :Pint;
     k:Pfloat;
     t|c|i|a :Pint;
    
PINST
Begin 
    /* merouane */
    //merouane
    x <-- 0 ;
     j<--x-y;
     x <-- 6/(x) ;
    FOR i <-- 1 WHILE 6
    DO
         FOR i <-- 1 WHILE 6
        DO
        DO i<--5; IF:(a+c <=1-t) ENDIF;
        ENDFOR;
    DO i<--5; IF:(x < j) ENDIF;
    ENDFOR;
    DO j<--5; IF: (!(jlll>5))
    ELSE j<--j/2; ENDIF;
End